================================================================================
    FREELANCE JOB ALERTS BOT - QUICK START GUIDE
================================================================================

🚀 SETUP (First time only - 10 minutes)

1. Get bot token:
   - Go to Telegram, message @BotFather
   - Send: /newbot
   - Follow prompts, save your token

2. Install dependencies:
   cd C:\Users\Joe\clawd\freelance-bot
   python3 -m pip install -r requirements.txt

3. Start the bot:
   set TELEGRAM_BOT_TOKEN=your_token_here
   python3 bot.py
   
   (You should see "Bot starting...")

4. Test in Telegram (message your new bot):
   /start → Welcome message
   /jobs → Get 5 jobs
   /filter → Set preferences
   /upgrade → See pricing

5. Setup cron job (runs hourly to fetch jobs):
   
   WINDOWS (Task Scheduler):
   - Open Task Scheduler
   - Create Basic Task
   - Name: "FreelanceBotWorker"
   - Trigger: Hourly
   - Action: Python script: C:\Users\Joe\clawd\freelance-bot\cron_worker.py
   - Env var: TELEGRAM_BOT_TOKEN=your_token

   LINUX/MAC (crontab):
   crontab -e
   0 * * * * cd ~/freelance-bot && TELEGRAM_BOT_TOKEN="token" python3 cron_worker.py

✅ DEPLOY

Bot is now:
- Listening for commands (/start, /jobs, /upgrade)
- Fetching jobs hourly (cron job)
- Sending alerts to premium users
- Tracking stats in SQLite database

Share your bot link: https://t.me/YourBotUsername

📊 DAILY OPERATIONS (Each morning)

1. Check stats:
   python3 stats.py
   
   Look for:
   - Total users growing?
   - Premium conversions?
   - Cron job running?

2. Check logs for errors:
   tail -f cron_worker.log

3. If bot crashed:
   ps aux | grep bot.py
   python3 bot.py &  (to restart)

🔧 TROUBLESHOOTING

Problem: Bot not responding
Solution: Restart it
  ps aux | grep bot.py (find process ID)
  kill <PID>
  python3 bot.py

Problem: No new jobs appearing
Solution: Check cron job
  Verify Task Scheduler / crontab has the job
  Run manually: python3 cron_worker.py
  Check cron_worker.log for errors

Problem: Too many users, bot slow
Solution: Check limits
  python3 stats.py (see user count)
  If >1000 users, upgrade to async handling

📈 MONITORING

Weekly check (15 min):
  python3 stats.py
  
  Key metrics:
  - Users (free vs premium %)
  - Jobs fetched (should increase)
  - Revenue (Telegram Stars → USD)
  - Conversion rate (5-10% target)

📱 USER MANAGEMENT

View all users:
  sqlite3 freelance_bot.db "SELECT telegram_id, subscription_level, created_at FROM users;"

Delete spam user:
  sqlite3 freelance_bot.db "DELETE FROM users WHERE telegram_id = 123456789;"

View premium users:
  sqlite3 freelance_bot.db "SELECT COUNT(*) FROM users WHERE subscription_level='premium';"

💰 REVENUE

Every premium user pays: 200 Telegram Stars ≈ $2/month
You keep: 70% = $1.40/month per user

Target: 10 paying users = $14/month
Goal: 100 paying users = $140/month (break-even on server)

Payment API ready to integrate when ready (code in bot.py)

🎯 GROWTH TACTICS (Week 1-4)

1. Share in Telegram groups:
   - r/remotework (if available)
   - Freelancer communities
   - Remote work subreddits
   (Each post = 50-200 signups)

2. Product Hunt:
   - Free listing
   - Can hit front page = 500+ signups

3. Reddit:
   - r/freelance, r/remotework
   - Honest recommendation (not spam)
   - Can reach 100-200 users

4. Referral system:
   - "Get 1 month free when you refer a friend"
   - Add to README/bot message
   - Viral growth potential

Target: 100-150 users by week 4
Target: 10 paying users by week 4

📚 FILES REFERENCE

bot.py                - Main bot (responds to commands)
cron_worker.py        - Job fetcher (runs hourly)
config.py             - Settings (pricing, limits)
stats.py              - Dashboard (metrics + health)
freelance_bot.db      - SQLite database (users, jobs, history)
DEPLOYMENT.md         - Full setup guide + troubleshooting
BUILD_REPORT.md       - This build's summary + next steps
README.md             - Overview + architecture

💡 TIPS

- Keep bot token secret (env var, not in code)
- Backup database weekly: cp freelance_bot.db freelance_bot.db.backup
- Monitor cron_worker.log for API issues
- Read DEPLOYMENT.md for advanced setup
- Join Telegram Bot Dev community for help

🆘 SUPPORT

If stuck, check:
1. DEPLOYMENT.md (full guide)
2. cron_worker.log (error messages)
3. stats.py (system health)
4. README.md (quick overview)

Questions? Build well-documented, run automated, scale fast.

================================================================================
Status: MVP Ready | First 10 Users: 2-4 weeks | Profitability: Clear
================================================================================
